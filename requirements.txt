# Core Streamlit and Data Processing
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.21.0
Pillow>=9.0.0
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.0.0

# Enhanced Navigation and UI
streamlit-option-menu>=0.3.6
# streamlit-webrtc has Python version compatibility issues with 3.13
# streamlit-webrtc>=0.45.0
streamlit-lottie>=0.0.3

# QR Code Generation
qrcode[pil]>=7.3.1

# Machine Learning (Basic)
# Use latest version compatible with Python 3.13
scikit-learn>=1.3.0

# Calendar and Visualization
# calendar is built-in to Python, no need to install
# calmap>=0.0.9  # Incompatible with Python 3.13 (requires distutils)

# Voice and Speech
pyttsx3>=2.90
SpeechRecognition>=3.8.1
# PyAudio - Audio I/O library for voice recognition
# Note: PyAudio requires system dependencies:
# - Windows: Usually works with pip, but may need Microsoft Visual C++ Build Tools
# - macOS: brew install portaudio
# - Ubuntu/Debian: sudo apt-get install portaudio19-dev python3-pyaudio
# - CentOS/RHEL: sudo yum install portaudio-devel
# Alternative: Use conda install pyaudio if pip fails
PyAudio>=0.2.11

# Computer Vision and OCR
# Use latest versions compatible with Python 3.13
opencv-python>=4.8.0
pytesseract>=0.3.10

# Security and Authentication
passlib>=1.7.4
bcrypt>=4.0.0
PyJWT>=2.4.0
pyotp>=2.6.0
cryptography>=3.4.8

# Payment Gateways
stripe>=5.0.0
razorpay>=1.3.0
paypalrestsdk>=1.13.1

# HTTP Requests
requests>=2.28.0

# Scheduling
APScheduler>=3.9.1

# Additional Utilities
python-dotenv>=0.19.0
psutil>=5.9.0
watchdog>=2.1.9
joblib>=1.2.0
jsonschema>=4.0.0
python-dateutil>=2.8.0
pytz>=2023.3